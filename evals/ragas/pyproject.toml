[project]
name = "ai-rag-pipeline-ragas-evals"
version = "0.1.0"
description = "Ragas-based mature RAG evaluation stack for the TypeScript/Bun AI RAG Pipeline artifact."
requires-python = ">=3.10,<3.13"
dependencies = [
  "datasets>=2.19.0",
  "langchain-openai>=0.2.0",
  "ragas>=0.2.0",
]

[tool.uv]
package = false
